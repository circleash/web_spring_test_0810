<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.circleash.spring.lesson03.dao.RealestateDAO">
 	<select id="selectRealestate" resultType="com.circleash.spring.lesson03.model.Realestate"
 	parameterType="int">
 		SELECT
 			`id`
 			, `realtorId`
 			, `address`
 			, `area`
 			, `type`
 			, `price`
 			, `rentPrice`
 			, `createdAt`
 			, `updatedAt`
 		FROM
 			`real_estate`
 		WHERE
 			`id` = #{id}
 	</select>
 	
 	<select id="selectRealEstateByRentPrice" resultType="com.circleash.spring.lesson03.model.Realestate"
 	parameterType="int">
	 	SELECT
	 			`id`
	 			, `realtorId`
	 			, `address`
	 			, `area`
	 			, `type`
	 			, `price`
	 			, `rentPrice`
	 			, `createdAt`
	 			, `updatedAt`
	 		FROM
	 			`real_estate`
	 		WHERE
	 			<![CDATA[`rentPrice` < #{rent}]]>
	 			
 	</select>
 	
 	<select id="selectRealEstateByAreaPrice" resultType="com.circleash.spring.lesson03.model.Realestate"
 	parameterType="int">
 		SELECT
	 			`id`
	 			, `realtorId`
	 			, `address`
	 			, `area`
	 			, `type`
	 			, `price`
	 			, `rentPrice`
	 			, `createdAt`
	 			, `updatedAt`
	 		FROM
	 			`real_estate`
	 		WHERE
	 			<![CDATA[
	 			`area` >= #{area}
	 			AND `price` <= #{price}
	 			]]>
 	</select>
 
 
 
 </mapper>