<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.circleash.spring.lesson05.dao.WeatherHistoryDAO_1">
 	<select id="selectWeatherHistory_1" resultType="com.circleash.spring.lesson05.model.Weather">
 		SELECT
 			`id`
 			, `date`
 			, `weather`
 			, `temperatures`
 			, `precipitation`
 			, `microDust`
 			, `windSpeed`
 			, `createdAt`
 			, `updatedAt`
 		FROM
 			`weatherhistory`
 	</select>
 	<insert id="insertWeather_1" parameterType="map">
		INSERT INTO
			`weatherhistory`
		( 
			`date`
 			, `weather`
 			, `temperatures`
 			, `precipitation`
 			, `microDust`
 			, `windSpeed`
 			, `createdAt`
 			, `updatedAt`
 		) VALUEs (
 			#{date}
 			, #{weather}
 			, #{temperatures}
 			, #{precipitation}
 			, #{microDust}
 			, #{windSpeed}
 			, now()
 			, now()
 		)
		
	
	</insert>
 
 
 
 </mapper>